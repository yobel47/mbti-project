/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: ['en', 'id'],
        defaultLocale: 'en',
        localeDetection: false,
      }
};

export default nextConfig;
